services:
  pinioverkill:
    build: .
    container_name: pinioverkill-game
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "description=PiniOverKill - Browser-based vertical scrolling shooter"
